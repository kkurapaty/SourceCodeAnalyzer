<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="UnderlineBrush.Color">#FF0078D7</Color>
    <Color x:Key="UnderlineHover.Color">#FF434343</Color>
    <Color x:Key="TabItem.Foreground.Color">#FF666666</Color>
    <!-- Brush definitions -->
    <SolidColorBrush x:Key="TabItem.Static.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="TabItem.Static.Border" Color="Transparent"/>
    <SolidColorBrush x:Key="TabItem.MouseOver.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="TabItem.MouseOver.Border" Color="Transparent"/>
    <SolidColorBrush x:Key="TabItem.Selected.Background" Color="Transparent"/>
    <SolidColorBrush x:Key="TabItem.Selected.Border" Color="Transparent"/>
    <SolidColorBrush x:Key="TabItem.Selected.Foreground" Color="{StaticResource UnderlineBrush.Color}"/>
    <SolidColorBrush x:Key="TabItem.Foreground" Color="{StaticResource TabItem.Foreground.Color}"/>
    <SolidColorBrush x:Key="UnderlineBrush" Color="{StaticResource UnderlineBrush.Color}"/>
    <SolidColorBrush x:Key="UnderlineHoverBrush" Color="{StaticResource UnderlineHover.Color}"/>

    <!-- TabControl Style -->
    <Style x:Key="Style.TabControl.Flat" TargetType="{x:Type TabControl}">
        <Setter Property="Padding" Value="4,2"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabControl}">
                    <Grid KeyboardNavigation.TabNavigation="Local">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- TabPanel with border at bottom -->
                        <TabPanel x:Name="HeaderPanel"
                                  Grid.Row="0"
                                  Panel.ZIndex="1"
                                  Margin="0,0,0,0"
                                  IsItemsHost="True"
                                  KeyboardNavigation.TabIndex="1"
                                  Background="Transparent"/>

                        <!-- Content -->
                        <Border Grid.Row="1"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                            ContentSource="SelectedContent"
                                            TextElement.Foreground="{TemplateBinding Foreground}"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TabItem Style -->
    <Style x:Key="Style.TabItem.Flat" TargetType="{x:Type TabItem}">
        <Setter Property="Foreground" Value="{StaticResource TabItem.Foreground}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Background" Value="{StaticResource TabItem.Static.Background}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TabItem.Static.Border}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Grid>
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="Content" 
                                              ContentSource="Header" TextElement.Foreground="Black"
                                              HorizontalAlignment="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                              VerticalAlignment="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                              RecognizesAccessKey="True"/>
                        </Border>

                        <!-- Underline indicator -->
                        <Border x:Name="Underline" Margin="2,0" Height="2" VerticalAlignment="Bottom" Background="Transparent" Width="0">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="0" />
                            </Border.RenderTransform>
                            <Border.Triggers>
                                <!-- Selected state animation -->
                                <EventTrigger RoutedEvent="Selector.Selected">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                            To="{StaticResource UnderlineBrush.Color}"
                                                            Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <!-- Unselected state animation -->
                                <EventTrigger RoutedEvent="Selector.Unselected">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                            To="Transparent"
                                                            Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>

                                <EventTrigger RoutedEvent="Selector.MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetProperty="Background.Color"
                                                            To="{StaticResource UnderlineHover.Color}"
                                                            Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Border.Triggers>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <!-- MouseOver trigger -->
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Foreground" Value="{StaticResource TabItem.Selected.Foreground}"/>
                            <Setter TargetName="Underline" Property="Background" Value="{StaticResource UnderlineHoverBrush}"/>
                            <Setter TargetName="Underline" Property="Opacity" Value="0.5"/>
                            <Setter TargetName="Underline" Property="Width" Value="Auto" />
                            <Setter TargetName="Underline" Property="RenderTransform">
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <!-- Selected trigger -->
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="Foreground" Value="{StaticResource TabItem.Selected.Foreground}"/>
                            <Setter TargetName="Underline" Property="Background" Value="{StaticResource UnderlineBrush}"/>
                            <Setter TargetName="Underline" Property="Opacity" Value="1.0"/>
                            <Setter TargetName="Underline" Property="Width" Value="Auto" />
                            <Setter TargetName="Underline" Property="RenderTransform" >
                                <Setter.Value>
                                    <ScaleTransform ScaleX="1" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TabItem}" BasedOn="{StaticResource Style.TabItem.Flat}"/>
    <Style TargetType="{x:Type TabControl}" BasedOn="{StaticResource Style.TabControl.Flat}"/>

</ResourceDictionary>